<?xml version="1.0" encoding="UTF8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aqiang.dao.BlogMapper">
    <select id="getBlogs" resultType="blog">
        select * from blog;
    </select>

<!--    if语句-->
    <select id="queryBlogIf" resultType="blog" parameterType="map">
        select * from blog
        <where>
            <if test="title != null">
                title = #{title}
            </if>
            <if test="author != null">
                and author = #{author}
            </if>
        </where>
    </select>

<!--set语句-->
    <update id="updateBlog" parameterType="map">
        update blog
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="author != null">
                author = #{author}
            </if>
        </set>
        where id = #{id};
    </update>

<!-- choose语句   -->
    <select id="queryBlogChoose" parameterType="map" resultType="blog">
        select * from blog
        <where>
            <choose>
                <when test="title != null">
                    title = #{title}
                </when>
                <when test="author != null ">
                    author = #{author}
                </when>
                <otherwise>
                    and views = #{views}
                </otherwise>
            </choose>
        </where>
    </select>


    <!--foreach语句,需求：查询表中id分别为1,2,3的数据
      select * from blog (id=1 or id= 2 or id =3 )-->
    <select id="queryBlogForeach" parameterType="map" resultType="blog">
        select  * from blog
        <where>
            <foreach collection="ids" item="id" open="(" close=")" separator="or">
                id = #{id}
            </foreach>
        </where>
    </select>

   <!-- &lt;!&ndash;开启二级缓存，默认参数&ndash;&gt;
    <cache/>
    &lt;!&ndash;开启二级缓存，设置参数&ndash;&gt;
    <cache eviction="FIFO" flushInterval="60000" size="512" readOnly="true"/>-->
</mapper>