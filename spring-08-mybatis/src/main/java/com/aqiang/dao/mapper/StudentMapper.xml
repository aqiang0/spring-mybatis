<?xml version="1.0" encoding="UTF8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aqiang.dao.demo01.StudentMapper">
    <!--插入数据-->
    <insert id="insertStudent" parameterType="map">
        insert into student(id,name,tid) values(#{id},#{name},#{tid});
    </insert>
    <!--多对一情况
        方法一：查询嵌套处理
        方法二：结果嵌套处理
    -->
    <!--方法一：-->
    <!--<select id="getStudents" resultMap="getStudents">
        select * from student;
    </select>
    <resultMap id="getStudents" type="student">
        <association property="teacher" javaType="com.aqiang.pojo.demo01.Teacher" column="{id=tid,name=tid}" select="getTeacher"/>
    </resultMap>
    <select id="getTeacher" resultType="teacher">
        select * from teacher where id = #{id};
    </select>-->

    <!--方法二-->
    <select id="getStudents" resultMap="getStudents">
        select s.id sid,s.name sname,t.name tname,t.id tid
        from student s ,teacher t
        where s.tid = t.id;
    </select>
    <resultMap id="getStudents" type="student">
        <id property="id" column="sid"/>
        <result property="name" column="sname"/>
        <association property="teacher" javaType="Teacher">
            <result property="name" column="tname"/>
            <result property="id" column="tid"/>
        </association>
    </resultMap>

</mapper>